# Run identification
run_name: "baseline_interpatient_v1"

# Data
split: "inter_patient"

# Model
model: "baseline"
d_model: 128
dropout: 0.1

# Loss
loss: "weighted_ce"

# Training
batch_size: 64
lr: 0.0005
weight_decay: 0.0001
warmup_epochs: 5
max_epochs: 100
patience: 15